#include <bits/stdc++.h>
using namespace std;
int main(){
    int num;
    cin >> num;
    for (int k=0; k<num; k++){
        int a, b; 
        cin >> a >> b;
        cout << "Case " << k+1 << ": " << a+b << "\n";
    }
}